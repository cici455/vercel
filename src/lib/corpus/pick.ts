export function pickLine(lines: readonly string[], seed: string) {
  let h = 2166136261;
  for (let i = 0; i < seed.length; i++) {
    h ^= seed.charCodeAt(i);
    h = Math.imul(h, 16777619);
  }
  return lines[Math.abs(h) % lines.length];
}
